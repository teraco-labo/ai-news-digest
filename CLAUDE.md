# このプロジェクトについて

「世界一わかりやすいAIニュース」を毎朝自動で作って配信するシステム。
あわせて、セミナー録画から議事録を作るツールも入っている。

## 話し方（最優先）

**このプロジェクトの持ち主はエンジニアではない。** アイデアと運営が本業で、
コードは Claude に任せている。技術用語を説明なしに使うと、そこで話が止まる。

- 専門用語は**その場で、一言そえて**使う。
  ×「このリポジトリで」 → ○「このフォルダ（プロジェクト一式が入っている場所）で」
  ×「mainにマージします」 → ○「お試し版で作ったものを完成版に合流させます」
- 「ご存じだと思いますが」は禁物。知らない前提で書いて、知っていたら読み飛ばせばいい。
  逆は取り返しがつかない
- 手順を渡して終わりにしない。**こちらで実行できることは実行する。**
  ターミナルにコマンドを打たせるのは最後の手段
- 選択肢を並べるだけで終わらせない。おすすめを先に言って、理由を1行添える
- 一度説明した用語でも、間が空いたら軽く言い添える。覚えていて当然という顔をしない

要するに、**相手が詰まる場所を先回りして埋める**。それがこのプロジェクトでの仕事の一部。

## 見た目の変更は、必ずプレビューで見せる

サイトの見た目に関わる変更をしたら、**毎回、以下の2つをセットで提示する**こと。
ファイルを添付で送るのは禁止（受け取り側でダウンロード扱いになり、開けないため）。

1. **プレビュー（アーティファクト）**: https://claude.ai/code/artifact/3dcc7ea2-521f-4cc9-a8f8-17cc625a3b2e
   - 必ずこの**同じURLに上書き更新**する（新しいアーティファクトを作らない）
   - 作り方: `index.html` の相対リンクを本番の絶対URLに書き換え、
     上部にプレビューである旨の帯を付けたコピーを発行する（rerender等の後に）
2. **本番URL**: https://teraco-labo.github.io/ai-news-digest/
   - push後1〜2分で反映される。音声の再生確認はこちらでしかできない

## 用語（このプロジェクトでよく出るもの）

| 言葉 | かみくだくと |
|---|---|
| リポジトリ | プロジェクトのファイル一式が入ったフォルダ。GitHub（ネット上）に本体があり、Macにコピーを置いて作業する |
| ブランチ | 同じフォルダの「作業用の別バージョン」。本番を壊さないように分けてある。`main` が完成版 |
| コミット | 変更に名前を付けて記録すること。作業のセーブポイント |
| プッシュ | Macでの変更を GitHub の本体に送ること |
| マージ | 作業用バージョンの変更を、完成版に合流させること |
| プルリクエスト | 「この変更を完成版に入れていいですか」という確認の場。GitHub上で中身を見られる |
| MCP | Claude に外部のツールをつなぐ仕組み |
| API | プログラム同士をつなぐ窓口。使うと従量課金になることが多い |

## 主なファイル

| ファイル | 何をするもの |
|---|---|
| `generate_news.py` | 毎朝のニュースダイジェストを作る（全工程） |
| `curate.py` | 記事の選別と日本語化を Claude に任せる部分 |
| `glossary.py` | 記事中の専門用語に解説を付ける |
| `generate_podcast*.py` | ポッドキャストの台本と音声 |
| `seo_builder.py` | サイト全体（トップ・アーカイブ・RSS等）を作り直す |
| `seminar_notes.py` | セミナー録画から文字起こしと議事録を作る |

経緯と現状は `PROJECT_LOG.md`、セミナー議事録の詳細は `SEMINAR_NOTES.md`。

## Claude の呼び出し方

`claude` CLI（サブスクリプションの枠）を優先し、無ければ `ANTHROPIC_API_KEY` の
従量課金にフォールバックする。実装は `curate.py` の `_curate_via_cli` が基準。
新しくClaudeを呼ぶ処理を書くときは、この2段構えを踏襲する。
API を使ったら `api_cost_calculator.record_anthropic_usage` で記録すること。
持ち主は費用を気にしているので、黙って課金を増やさない。

## 書き方

- コード中のコメント・ドキュメント・コミットメッセージは日本語
- コミットメッセージは「何をしたか」だけでなく**「なぜそうしたか」**を書く。
  半年後に読み返して判断の理由が分かることを基準にする
- 記事やサイトの文章は敬体（です・ます）で統一する
